# Optional: full URL to API when not using CRA dev proxy (e.g. production)
# REACT_APP_API_URL=https://api.example.com

# Google Identity Services (same OAuth client id as backend GOOGLE_CLIENT_ID)
# REACT_APP_GOOGLE_CLIENT_ID=
